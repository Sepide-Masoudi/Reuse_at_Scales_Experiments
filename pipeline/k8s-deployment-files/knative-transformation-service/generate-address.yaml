apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: generate-address
  namespace: pipeline
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/class: kpa.autoscaling.knative.dev
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "3"
        autoscaling.knative.dev/target: "1"
    spec:
      containers:
        - name: generate-address
          image: sepidmas/reuse_scale-generate-address:knative
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
          volumeMounts:
            - name: argo-shard
              mountPath: /mnt/data/argo-shard
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 2
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
      volumes:
        - name: argo-shard
          persistentVolumeClaim:
            claimName: argo-shard-pvc
