apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: data-preprocess
  namespace: pipeline
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/class: kpa.autoscaling.knative.dev
        autoscaling.knative.dev/minScale: "0"
        autoscaling.knative.dev/maxScale: "3"
        autoscaling.knative.dev/target: "1"
    spec:
      containers:
        - name: data-preprocess
          image: sepidmas/reuse_scale-data-preprocessor:knative
          imagePullPolicy: Always
          ports:
            - containerPort: 8083
          volumeMounts:
            - name: argo-shard
              mountPath: /mnt/data/argo-shard
          readinessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 2
            periodSeconds: 5
            successThreshold: 1
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8083
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3              
      volumes:
        - name: argo-shard
          persistentVolumeClaim:
            claimName: argo-shard-pvc
