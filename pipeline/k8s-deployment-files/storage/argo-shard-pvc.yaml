apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: argo-shard-pvc
  namespace: pipeline
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  storageClassName: zfs
